# GeoScout Pro 按钮全量巡检清单

## 1. 登录页

- 轮播切换（左右箭头、圆点）：本地交互，已生效。
- 登录按钮：已接后端 `POST /api/auth/login`，成功后写入 `token` 并加载后台数据。

## 2. 侧边栏主导航

- 首页、卫星图检索、街景图检索、历史、AI、设置、系统面板、训练、数据库、用户管理：均为页面切换按钮，已生效。
- 退出登录：已清理 `token` 并回到登录态。

## 3. 首页

- 顶部刷新按钮：已改为重新拉取后端数据（不再整页刷新）。
- 日历卡片、系统介绍卡片展开按钮：本地交互，已生效。
- “开始检索”与历史弹窗等：本地交互，已生效。

## 4. 系统面板（内容管理）

- 新增任务、删除任务、编辑任务字段：本地编辑，已生效。
- 编辑系统介绍（名称/副标题/描述/核心功能）：本地编辑，已生效。
- 保存内容：已接后端并落库：
  - `PUT /api/calendar/tasks`
  - `PUT /api/system/intro`

## 5. 系统面板（日志）

- 日志列表展示：已接后端 `GET /api/system/logs`。

## 6. 训练中心

- 开始训练/重新启动：已接后端 `POST /api/training/start`。
- 终止训练：已接后端 `POST /api/training/fail`。
- 训练状态轮询：已接后端 `GET /api/training/status`（3 秒轮询）。
- 获取历史记录：已接后端 `GET /api/training/records`。
- 训练详情页：已从真实训练记录展示状态（进行中/已完成/已终止）。

## 7. 数据库管理

- 上传文件：已接后端 `POST /api/files/upload`，写入文件表并落盘。
- 删除文件：已接后端 `DELETE /api/files/:fileName`。
- 文件列表：已接后端 `GET /api/files`。

## 8. 用户管理

- 新增用户：已接后端 `POST /api/users`。
- 编辑用户：已接后端 `PUT /api/users/:username`。
- 删除用户：已接后端 `DELETE /api/users/:username`。
- 用户列表：已接后端 `GET /api/users`。

## 9. 设置页

- 修改头像与保存更改：已接后端 `PUT /api/users/:username`（当前同步头像等可映射字段）。

## 10. AI 页

- 发送消息按钮：已接 Gemini（由前端直连模型，不经本次管理后端）。
- 图片上传、快捷问题按钮：本地交互 + 调用 AI 发送逻辑，已生效。

